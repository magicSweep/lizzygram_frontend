import { ThemeState } from "./theme/store/types";

export interface GlobalState {
  /* modal: IModalState;
    alert: IAlertState;
    auth: IAuthState;
    tags: ITagsState;
    photos: IPhotosState;
    search: ISearchState; */
  theme: ThemeState;
}
